map:
1,若对象作为key，
  重载<来实现排序比较或者自定义函数对象类用于比较
2,若类指针作为key, 自定义函数对象类用于比较
3,若要通过值来排序，则可以将map中的数据提取到list中
  通过list中的sort函数来排list中的数据，然后显示

map的内部实现是通过红黑树来实现的

存放的数据是pair对象，pair对象中存放一对数据
第一个数据称之为key,第二个数据称之为值
将其称之为 键-值 对

插入数据时，自动根据key从大到小进行排序

map中key的值唯一，不能重复

